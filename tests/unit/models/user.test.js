const { User } = require("../../../app/models/user");
const jwt = require('jsonwebtoken');
const config = require('config');


describe('user.generateAuthtoken', () => {
    it('it should return jwt', () => {
        const payload = {}
    });
});